This directory stores feedback uploads.

Sub-folders are created automatically by the server:
  - audio/    → audio recordings + attached images
  - video/    → screen recordings
  - text/     → text feedback attached images
  - image/    → standalone image uploads

Each submission also generates a [id]-manifest.json file alongside the media files.
